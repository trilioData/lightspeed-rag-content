id: backup-yaml-full
category: backup
validation_mode: yaml_baseline
prompt: Generate a YAML for a Full backup named 'app-full-backup' that references a BackupPlan named 'app-backupplan' in namespace 'production'.
expected_citations:
- tvk-content/CRDs/backup-crd.md
yaml_validation:
- expected_kind: Backup
  expected_api_version: triliovault.trilio.io/v1
  expected_parameters:
    metadata.name: app-full-backup
    metadata.namespace: production
    spec.type: Full
    spec.backupPlan.name: app-backupplan
    spec.backupPlan.namespace: production
tags:
- cr-creation
- backup
- yaml
- full-backup
